<view class="cu-list menu sm-border">
    <block wx:for="{{musics}}" wx:key="{{item.id}}">
        <view class="cu-item">
            <view class="content">
                <music-item index="{{index}}" music="{{item}}" />
            </view>
        </view>
    </block>
</view>
