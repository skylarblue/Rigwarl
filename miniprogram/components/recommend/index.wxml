<!--components/recommend/index.wxml-->
<view class="recommend-wrap">
    <text class="recommend-title">推荐歌单</text>
    <view class="recommend-body">
        <block wx:for="{{list}}" wx:key="{{index}}">
            <music-card music="{{item}}" />
        </block>
    </view>
</view>
